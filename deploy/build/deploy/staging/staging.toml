# confspawn recipe file
# Sources not just its own, but also includes db, kv and server deployment scripts

config = "config.toml"

[[sources]]
source = "build/deploy/compose"
target = "use/staging"

[[sources]]
source = "build/deploy/staging"
target = "use/staging"