[project]
name = "apiserver"
version = "4.0.0"
description = "D.S.A.V. Dodeka backend"
authors = [
    { name = "Tip ten Brink", email = "75669206+tiptenbrink@users.noreply.github.com" },
    { name = "Matthijs Arnoldus", email = "mat.arnoldus@kpnmail.nl" },
    { name = "Leander Bindt", email = "leander.bindt@ziggo.nl" },
    { name = "Senne Drent", email = "sennedrent@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard-no-fastapi-cloud-cli]==0.116.1",
    "pydantic==2.11.7",
    "sqlalchemy",
    "jinja2>=3.1.6,<4.0.0",
    "loguru>=0.7.2,<1.0.0",
    "pydantic-settings>=2.10.1",
    "tiauth_faroe.user_server",
    "tiauth_faroe.client",
    "starlette==0.47.3",
    "httpx>=0.28.1,<0.29.0",
]

[tool.uv.sources]
"tiauth_faroe.user_server" = { git = "https://github.com/tiptenbrink/tiauth-faroe", subdirectory = "py-user-server/lib" }
"tiauth_faroe.client" = { git = "https://github.com/tiptenbrink/tiauth-faroe", subdirectory = "py-user-server/client", rev="61731ef0f257ee8cb95a68624e04f231495353ce" }
sqlalchemy = { git = "https://github.com/sqlalchemy/sqlalchemy.git", rev = "8bb8c1a6d6439380f682a57b92e3d1b0092250eb" }
# sqlalchemy = { git = "https://github.com/tiptenbrink/sqlalchemy.git", rev = "13f569fd615485c50e07f211e37e45d749ccc40d" }

[project.scripts]
backend = "apiserver.dev:run"

[dependency-groups]
dev = [
    "pytest>=8.4.2,<9.0.0",
    # "pytest-asyncio>=0.23.2,<0.24.0",
    # "pytest-mock>=3.7.0,<4.0.0",
    # "pre-commit>=2.20.0,<3.0.0",
        "alembic>=1.12.0,<2.0.0",
    # "coverage>=7.3.2,<8.0.0",
    # "black==23.11.0",
    # "mypy>=1.5.1,<2.0.0",
    # "faker>=19.3.1,<20.0.0",
    "ruff>=0.12.12,<0.13.0",
    # "types-regex>=2023.10.3.0,<2024.0.0",
]

# [tool.hatch.build.targets.wheel]
# only-packages = true
# packages = ["src/apiserver", "src/auth", "src/datacontext", "src/schema", "src/store"]

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"

# [tool.black]
# target-version = ['py311']
# preview = true

# [tool.mypy]
# python_version = "3.11"
# strict = true
# files = ["src"]
# plugins = [
#     "pydantic.mypy",
# ]

# [[tool.mypy.overrides]]
# module = [
#     "schema.model.env",
#     "schema.model.versions.*"
# ]
# ignore_errors = true

[tool.ruff.lint]
select = ["F", "PL", "E", "W", "RUF"]
# We ignore too-many-arguments and line length, as black fixes line length for code
# TODO: check if we need this
# ignore = ["PLR0913", "E501"]

[tool.ruff.lint.per-file-ignores]
# TODO: check if we need this
# "__init__.py" = ["F403"]

[tool.ruff.lint.pycodestyle]
# We allow docs and comments to be long
max-doc-length = 120


[tool.alembic]

# path to migration scripts.
# this is typically a path given in POSIX (e.g. forward slashes)
# format, relative to the token %(here)s which refers to the location of this
# ini file
script_location = "%(here)s/alembic"

# template used to generate migration file names; The default value is %%(rev)s_%%(slug)s
# Uncomment the line below if you want the files to be prepended with date and time
# see https://alembic.sqlalchemy.org/en/latest/tutorial.html#editing-the-ini-file
# for all available tokens
# file_template = "%%(year)d_%%(month).2d_%%(day).2d_%%(hour).2d%%(minute).2d-%%(rev)s_%%(slug)s"

# additional paths to be prepended to sys.path. defaults to the current working directory.
prepend_sys_path = [
    "."
]

# timezone to use when rendering the date within the migration file
# as well as the filename.
# If specified, requires the python>=3.9 or backports.zoneinfo library and tzdata library.
# Any required deps can installed by adding `alembic[tz]` to the pip requirements
# string value is passed to ZoneInfo()
# leave blank for localtime
# timezone =

# max length of characters to apply to the "slug" field
# truncate_slug_length = 40

# set to 'true' to run the environment during
# the 'revision' command, regardless of autogenerate
# revision_environment = false

# set to 'true' to allow .pyc and .pyo files without
# a source .py file to be detected as revisions in the
# versions/ directory
# sourceless = false

# version location specification; This defaults
# to <script_location>/versions.  When using multiple version
# directories, initial revisions must be specified with --version-path.
# version_locations = [
#    "%(here)s/alembic/versions",
#    "%(here)s/foo/bar"
# ]


# set to 'true' to search source files recursively
# in each "version_locations" directory
# new in Alembic version 1.10
# recursive_version_locations = false

# the output encoding used when revision files
# are written from script.py.mako
# output_encoding = "utf-8"

# This section defines scripts or Python functions that are run
# on newly generated revision scripts.  See the documentation for further
# detail and examples
# [[tool.alembic.post_write_hooks]]
# format using "black" - use the console_scripts runner,
# against the "black" entrypoint
# name = "black"
# type = "console_scripts"
# entrypoint = "black"
# options = "-l 79 REVISION_SCRIPT_FILENAME"
#
# [[tool.alembic.post_write_hooks]]
# lint with attempts to fix using "ruff" - use the module runner, against the "ruff" module
# name = "ruff"
# type = "module"
# module = "ruff"
# options = "check --fix REVISION_SCRIPT_FILENAME"
#
# [[tool.alembic.post_write_hooks]]
# Alternatively, use the exec runner to execute a binary found on your PATH
# name = "ruff"
# type = "exec"
# executable = "ruff"
# options = "check --fix REVISION_SCRIPT_FILENAME"
