# Auth server settings for local development/testing

# Database configuration
FAROE_DB_PATH=./envs/test/db.sqlite

# Server configuration
FAROE_PORT=12770

# Port for management commands (on 127.0.0.2)
FAROE_COMMAND_PORT=12771

# Port where the user server listens (on 127.0.0.2)
# This has to match the backend's private port
FAROE_USER_SERVER_PORT=12790

# Session configuration
# FAROE_SESSION_EXPIRATION: Absolute expiration time for sessions (default: 2160h = 90 days)
# Examples: 720h (30 days), 2160h (90 days), 4320h (180 days)
# FAROE_SESSION_EXPIRATION=2160h

# CORS configuration
FAROE_CORS_ALLOW_ORIGIN=http://localhost:3000
