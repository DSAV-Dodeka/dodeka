allowed_envs = [{{ conf_env.allowed_envs }}]

api_root = "{{ conf_env.credentials_url }}"
issuer = "{{ server.issuer }}"
frontend_client_id = "{{ server.frontend_client_id }}"
backend_client_id = "{{ server.backend_client_id }}"

valid_redirects = [{{ conf_env.valid_redirects }}]

credentials_url = "{{ conf_env.credentials_url }}"

signup_url = "{{ server.signup_url }}"

onboard_email = "{{ server.onboard_email }}"
smtp_server = "{{ server.smtp_server }}"
smtp_port = {{ server.smtp_port }}